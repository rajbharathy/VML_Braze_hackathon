{
  "name": "braze-copilot",
  "version": "0.1.0",
  "description": "AI-native Braze practitioner — Hackathon skeleton",
  "main": "server/proxy.js",
  "scripts": {
    "start": "node server/proxy.js",
    "dev": "cross-env DOTENV_CONFIG_PATH=config.env node -r dotenv/config server/proxy.js"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^17.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
